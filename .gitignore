# Disk images — large binaries, user-provided or generated locally
disks/*.img
disks/*.iso
disks/*.dmg
disks/*.cdr
disks/*.qcow2

# Game and OS archives — user-provided
*.sit
*.hqx
*.bin

# Vendored QEMU + tools — built by 'make vendor', not committed
vendor/qemu/

# macOS
.DS_Store
**/.DS_Store
